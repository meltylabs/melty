import * as conversations from "../backend/conversations";

suite('encode context', () => {
    // never mind, we're just going to cheat on final newlines

    // test('should preserve correct number of final newlines', () => {
    //     const context = "hello";
    //     const encoded = conversations.encodeContext(context);
    //     assert.strictEqual(encoded, "```\nhello```");
    // });
});
